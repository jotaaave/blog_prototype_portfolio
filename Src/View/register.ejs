<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Blog - Login</title>
</head>
<body>
  <section class="container">
    <h1>Faça seu Registro!</h1>

    <h3 style="color: red;">
      <% if (locals.messages.errMsgArray) { %>
        <% for (const i in locals.messages.errMsgArray) { %>
          <%= locals.messages.errMsgArray[i] %> %> <br>
        <% } %>
      <% } %>
    </h3>

    <div class="form">
      <form action="/register" class="signForm" method="post">
        <label for="user">Usuário:</label>
        <input type="text" name="user" id="user" value="<%= locals.messages.user %>"> <br><br>

        <label for="email">Email:</label>
        <input type="text" name="email" id="email" value="<%= locals.messages.email %>"> <br><br>

        <label for="password">Senha:</label>
        <input type="text" name="password" id="password" value="<%= locals.messages.password %>"> <br><br>

        <button class="sendLogin">Enviar</button>
      </form>
    </div>
  </section>
  <script src="assets/js/bundle.js"></script>
</body>
</html>
